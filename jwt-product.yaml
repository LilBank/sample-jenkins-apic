info:
  title: ocb-api-test-1
  name: ocb-api-test-1
  version: 1.0.0
apis:
  jwt1.0.0:
    $ref: jwt-api.yaml
gateways:
  - datapower-api-gateway
plans:
  default-plan:
    title: Test plan
    description: If you see this product published, then it works
    approval: false
    rate-limits:
      default:
        value: 100/1hour
    apis:
      jwt1.0.0: {}
product: 1.0.0
visibility:
  view:
    type: public
    enabled: true
  subscribe:
    enabled: true
    type: authenticated
